(function(){
  'use strict';
  var menu=document.getElementById('trade-menu'),nav=document.getElementById('trade-links');
  if(menu&&nav){menu.addEventListener('click',function(){var open=nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',open?'true':'false');});document.addEventListener('keydown',function(event){if(event.key==='Escape'&&nav.classList.contains('is-open')){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');menu.focus();}});}
  document.querySelectorAll('[data-factory-faq] details').forEach(function(item){item.addEventListener('toggle',function(){if(!item.open)return;document.querySelectorAll('[data-factory-faq] details').forEach(function(other){if(other!==item)other.open=false;});});});
  var modes={manufacture:{code:'MFG',title:'制造条件先决定工艺路径',items:['提供 2D 图纸与 3D 模型','标明材质、板厚和表面处理','说明样品及年度预计数量']},quality:{code:'QC',title:'把验收标准写进订单之前',items:['列出关键尺寸与特殊公差','确认抽样水平和测试标准','说明随货报告与追溯要求']},export:{code:'EXP',title:'交付条件影响包装与报价',items:['提供目的国、城市或港口','确认贸易条款与期望到货日','说明包装、标签与认证要求']}};
  var tabs=[].slice.call(document.querySelectorAll('[data-trade-mode]')),panel=document.querySelector('.trade-result'),code=document.getElementById('trade-code'),title=document.getElementById('trade-title'),items=document.getElementById('trade-items');
  if(panel){panel.id='trade-results';panel.setAttribute('role','tabpanel');panel.setAttribute('aria-live','polite');panel.setAttribute('aria-atomic','true');}
  tabs.forEach(function(tab,index){tab.id='trade-mode-'+(index+1);tab.setAttribute('aria-controls','trade-results');tab.setAttribute('tabindex',index===0?'0':'-1');});
  function render(key){var data=modes[key];if(!data||!code||!title||!items)return;code.textContent=data.code;title.textContent=data.title;items.replaceChildren();data.items.forEach(function(text){var li=document.createElement('li');li.textContent=text;items.appendChild(li);});tabs.forEach(function(tab){var active=tab.getAttribute('data-trade-mode')===key;tab.classList.toggle('is-active',active);tab.setAttribute('aria-selected',active?'true':'false');tab.setAttribute('tabindex',active?'0':'-1');if(active&&panel)panel.setAttribute('aria-labelledby',tab.id);});}
  function move(current,event){var index=tabs.indexOf(current),next=index;if(event.key==='ArrowRight'||event.key==='ArrowDown')next=(index+1)%tabs.length;else if(event.key==='ArrowLeft'||event.key==='ArrowUp')next=(index-1+tabs.length)%tabs.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=tabs.length-1;else return;event.preventDefault();render(tabs[next].getAttribute('data-trade-mode'));tabs[next].focus();}
  tabs.forEach(function(tab){tab.addEventListener('click',function(){render(tab.getAttribute('data-trade-mode'));});tab.addEventListener('keydown',function(event){move(tab,event);});});
  if(tabs.length)render(tabs[0].getAttribute('data-trade-mode'));
}());

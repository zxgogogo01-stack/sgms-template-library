# 018-raftwire

## 定位

高端实时资讯线 / 快讯编辑台模板。视觉以深海蓝、信号青、警示橙和高密度时间信息为核心，适合行业快讯、产品更新、研究动态、状态播报与公开事件记录站。

## 页面套件

- `index.html`：资讯台首屏、今日统计、邀请码、主题筛选、六条完整快讯、编发原则入口
- `article.html`：日度归档、归档摘要、五条带来源与核对状态的记录
- `tool.html`：秒/毫秒 Unix 时间戳解码，本地时间、UTC、ISO 8601 与复制反馈
- `legal.html`：来源、核对、更正、内容边界与联系说明
- `404.html`：信号中断主题错误页
- `css/style.css`：统一设计系统、桌面/手机布局、深色模式、焦点和减弱动效

## 占位符

`%SITE_NAME%`、`%SITE_DOMAIN%`、`%SITE_TAGLINE%`、`%SITE_DESC%`、`%INVITE_CODE%`、`%COMPLIANCE_NOTE%`、`%CONTACT_EMAIL%`、`%LANG%`

## 核心交互

- 首页可复制邀请码，并通过 `aria-live` 返回成功或失败状态
- 首页可按产品、研究、运营筛选快讯，并同步更新可见条目数量
- 时间工具会校验输入、聚焦首个错误字段，并生成本地时间、UTC、ISO、偏移与星期信息
- 时间工具支持复制 ISO 时间并返回状态

## 后续 AI 的文字接入顺序

这套快讯站的五页 UI、主题筛选、邀请码复制和时间戳工具都已完成。后续 AI 只替换占位符、快讯和说明文字，不要重写布局、CSS 类名或页内脚本：

1. 先在全部页面统一替换 8 个占位符，并同步页面标题、描述、结构化数据、日期、统计数字与联系方式。
2. 再替换 `index.html` 的六条演示快讯；每条必须保留 `data-topic`、主题、来源类型、核对状态、发布时间、时区和核对时间，增减后同步筛选按钮与总数。
3. 将 `article.html` 写成真实日度归档，保留更正记录与原始发布时间；所有页面使用一致时区，跨地区内容同时记录 UTC 或 ISO 8601。
4. 在 `tool.html` 只修改工具说明与默认示例，保留时间戳、单位、小时制、输出和复制控件 ID；在 `legal.html` 填写实际来源、核对、更正、内容边界和联系流程。
5. 完成后实测邀请码复制、全部/产品/研究/运营四类筛选，以及时间工具的空值、负数、小数、非安全整数、秒/毫秒、12/24 小时制、超范围、Unix 零点和 ISO 复制；增删页面或归档日期时同步导航与 `sitemap.xml`。

## 可删区块

- 首页 `access-strip` 邀请码条可删除，同时移除复制脚本与编发说明中的邀请码句子。
- 首页 `editor-note` 与统计栏可独立删除。
- `tool.html` 可删除，同时移除导航和站点地图中的对应地址。

## 验收范围

最终审计日期：2026-08-31。已检查首页、日度归档、时间工具、编发说明与 404 的桌面端、390px 手机端和深色模式；同时覆盖主题筛选、两类复制、时间戳全部错误与成功状态、键盘焦点、触控目标和横向溢出。

# 013-signal-digest

## 适合什么站

适合每周摘要、行业观察、规则追踪和公告归档。整套采用“铜色编辑部周刊”方向：深褐期数栏、暖纸编辑区、铜色校订标记、醒目的期号信息牌和来源说明，强调可核对、可归档、不过度下结论。

## 页面与内容结构

- `index.html`：刊头、本期信息牌、邀请码条、四条信号摘要与来源说明。
- `article.html`：完整往期归档，包含发布日期、条目状态、核对项和下期预告。
- `tool.html`：变化幅度计算，覆盖空值、期初为零、上涨和下跌结果。
- `legal.html`：编辑原则、来源修订、邀请码、声明与联系信息。
- `404.html`：延续“归档缺期”叙事的错误页。

## 视觉与交互

- 桌面端使用深色期数侧栏，窄屏转为可横向浏览、带滚动吸附的归档导航。
- 站点标识可返回最新一期；脚本自动标记当前页面。
- 邀请码复制按钮提供即时状态反馈。
- 变化计算器会在输入无效时标记字段、显示清晰错误并把焦点送回问题输入；有效结果区分上涨和下跌。
- 支持深色模式、清晰键盘焦点与减少动态效果偏好。

## 占位符清单（`%X%` 语法）

`%SITE_NAME%`、`%SITE_DOMAIN%`、`%SITE_TAGLINE%`、`%SITE_DESC%`、`%INVITE_CODE%`、`%COMPLIANCE_NOTE%`、`%CONTACT_EMAIL%`、`%LANG%`

## 后续 AI 的文字接入顺序

这套周刊的完整 UI、期号系统和交互均已搭好。后续 AI 只替换变量与文字内容，不要重建版式、改 CSS 类名或改写 `site.js`：

1. 先全局替换上面的 8 个占位符，确保域名、邀请码、合规说明和联系方式在全部页面保持一致。
2. 再填写 `index.html` 的期号、日期、导语、4 条本期信号和来源说明；增减条目时复制或删除 `.item`，并同步编号和条目总数。
3. 将 `article.html` 改为一整期可归档内容，保留发布日期、条目状态、每条核对项和下期预告，让读者能追溯来源与修订。
4. 在 `tool.html` 只修改说明与示例值，保留 `dv-a`、`dv-b`、`dv-go`、`dv-out`；在 `legal.html` 填写编辑原则、来源修订、邀请码披露、声明和联系信息。
5. 完成后实测邀请码复制，以及计算器的双空值、单字段缺失、期初为零、上涨、下跌和小数结果；再检查手机端长站名、归档导航与长数字无溢出。

## 可按需删减

- 邀请码区：删除首页 `.code-bar`，并删除关于页“邀请码”小节；脚本会在元素不存在时安全跳过。
- 来源说明：删除首页 `.src-note`。
- 条目：按需增删 `.item`，同步更新本期条目数与归档说明。
- 变化计算器：删除 `tool.html`，同时移除各页导航与 sitemap 对应项。

## 验收范围

最终审计日期：2026-08-31。已检查首页、往期、计算器、关于页与 404 的桌面端、手机端和深色模式；同时覆盖邀请码复制、计算器空值/单缺项/零值/上涨/下跌/小数结果、错误恢复、键盘焦点、触控目标和横向溢出。

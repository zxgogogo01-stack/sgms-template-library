(function(){
  'use strict';
  var menu=document.getElementById('eng-menu'),nav=document.getElementById('eng-links');
  if(menu&&nav){menu.addEventListener('click',function(){var open=nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',open?'true':'false');});nav.querySelectorAll('a').forEach(function(link){link.addEventListener('click',function(){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');});});document.addEventListener('keydown',function(event){if(event.key==='Escape'&&nav.classList.contains('is-open')){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');menu.focus();}});}
  var data={structure:{code:'STR',title:'结构参数定义装配关系',rows:[['外形','尺寸与安装方向'],['接口','开孔、门锁与进线'],['内部','安装板、导轨与附件']]},material:{code:'MAT',title:'材料参数定义制造路径',rows:[['基材','牌号、板厚与状态'],['公差','关键尺寸与基准'],['表面','颜色、纹理与膜厚']]},validation:{code:'VAL',title:'验证参数定义交付证据',rows:[['尺寸','抽样与判定范围'],['测试','标准、条件与时长'],['文件','报告、追溯与版本']]}};
  var tabs=[].slice.call(document.querySelectorAll('[data-eng-mode]')),panel=document.querySelector('.eng-result'),code=document.getElementById('eng-code'),title=document.getElementById('eng-title'),items=document.getElementById('eng-items');
  if(panel){panel.id='eng-results';panel.setAttribute('role','tabpanel');panel.setAttribute('aria-live','polite');panel.setAttribute('aria-atomic','true');}
  tabs.forEach(function(tab,index){tab.id='eng-mode-'+(index+1);tab.setAttribute('aria-controls','eng-results');tab.setAttribute('tabindex',index===0?'0':'-1');});
  function render(key){var item=data[key];if(!item||!code||!title||!items)return;code.textContent=item.code;title.textContent=item.title;items.replaceChildren();item.rows.forEach(function(row){var tr=document.createElement('tr'),th=document.createElement('th'),td=document.createElement('td');th.textContent=row[0];td.textContent=row[1];tr.append(th,td);items.appendChild(tr);});tabs.forEach(function(tab){var active=tab.getAttribute('data-eng-mode')===key;tab.classList.toggle('is-active',active);tab.setAttribute('aria-selected',active?'true':'false');tab.setAttribute('tabindex',active?'0':'-1');if(active&&panel)panel.setAttribute('aria-labelledby',tab.id);});}
  function move(current,event){var index=tabs.indexOf(current),next=index;if(event.key==='ArrowRight'||event.key==='ArrowDown')next=(index+1)%tabs.length;else if(event.key==='ArrowLeft'||event.key==='ArrowUp')next=(index-1+tabs.length)%tabs.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=tabs.length-1;else return;event.preventDefault();render(tabs[next].getAttribute('data-eng-mode'));tabs[next].focus();}
  tabs.forEach(function(tab){tab.addEventListener('click',function(){render(tab.getAttribute('data-eng-mode'));});tab.addEventListener('keydown',function(event){move(tab,event);});});
  if(tabs.length)render(tabs[0].getAttribute('data-eng-mode'));
}());

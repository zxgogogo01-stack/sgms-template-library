(function(){"use strict";var form=document.querySelector("[data-instrument]");if(!form)return;var kind=document.body.getAttribute("data-tool");var title=document.querySelector("[data-result-title]");var detail=document.querySelector("[data-result-detail]");var copy=document.querySelector("[data-result-copy]");var status=document.querySelector("[data-result-status]");var latest="";function setResult(head,body,error){title.textContent=head;detail.textContent=body;latest=error?"":head+" — "+body;copy.disabled=!!error;if(status)status.textContent=error?"请修正输入后重试。":"结果已生成，可复制。"}function value(name){return form.elements[name].value.trim()}function number(name){return Number(form.elements[name].value)}form.addEventListener("submit",function(event){event.preventDefault();if(kind==="pair"){var a=value("a"),b=value("b");if(!a||!b){setResult("缺少术语","两个术语都需要填写。",true);return}if(a.toLowerCase()===b.toLowerCase()){setResult("同一术语",a+" 与 "+b+" 归一化后相同；先确认是否需要拆分。",false);return}setResult("词对已拆分",a+" ↔ "+b+"：分别填写共同语境、关键差异和不可互换条件。",false)}else if(kind==="boundary"){var example=value("example"),scope=value("scope"),counter=value("counter");if(!example||!scope||!counter){setResult("字段不完整","例子、适用范围和反例缺一不可。",true);return}setResult("边界卡已排好","例子："+example+"；适用："+scope+"；不适用："+counter+"。",false)}else if(kind==="revision"){var start=new Date(value("review")+"T00:00:00Z"),end=new Date(value("current")+"T00:00:00Z"),max=number("max");if(Number.isNaN(start.getTime())||Number.isNaN(end.getTime())||!Number.isInteger(max)||max<0){setResult("日期或期限无效","填写有效日期与不小于零的整数天数。",true);return}var days=Math.round((end-start)/86400000);if(days<0){setResult("日期顺序有误","对照日期不能早于上次复核日期。",true);return}setResult(days<=max?"仍在复核窗内":"需要重新核对","已过去 "+days+" 天；设定期限为 "+max+" 天。",false)}else if(kind==="weight"){var p=number("primary"),s=number("secondary"),c=number("community");if([p,s,c].some(function(n){return !Number.isInteger(n)||n<0||n>5})){setResult("评分无效","三项评分都需为 0–5 的整数。",true);return}var weighted=(p*3+s*2+c)/30*100;setResult("加权读数 "+weighted.toFixed(1)+"%","一手来源权重 3，二手来源权重 2，社区线索权重 1。",false)}else{var total=number("total"),rounds=number("rounds"),repeats=number("repeats");if(!Number.isInteger(total)||!Number.isInteger(rounds)||!Number.isInteger(repeats)||total<1||total>500||rounds<1||rounds>20||repeats<0||repeats>10){setResult("计划输入无效","请使用字段标注范围内的整数。",true);return}var each=Math.ceil(total/rounds);var exposures=total+Math.ceil(total*.25)*repeats;setResult("每轮约 "+each+" 张","基础 "+total+" 张，按四分之一难词复现后约 "+exposures+" 次呈现。",false)}});form.addEventListener("reset",function(){setTimeout(function(){title.textContent="等待输入";detail.textContent="填写字段后运行工具。";latest="";copy.disabled=true;if(status)status.textContent="已恢复示例输入。"},0)});copy.addEventListener("click",function(){if(!latest)return;var done=function(){status.textContent="结果已复制。"};if(navigator.clipboard&&navigator.clipboard.writeText)navigator.clipboard.writeText(latest).then(done).catch(function(){status.textContent="复制失败，请手动选择结果。"});else status.textContent="请手动选择结果复制。"})})();

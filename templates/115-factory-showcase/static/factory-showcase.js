(function(){
  'use strict';
  var menu=document.getElementById('show-menu'),nav=document.getElementById('show-links');
  if(menu&&nav){menu.addEventListener('click',function(){var open=nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',open?'true':'false');});nav.querySelectorAll('a').forEach(function(link){link.addEventListener('click',function(){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');});});document.addEventListener('keydown',function(event){if(event.key==='Escape'&&nav.classList.contains('is-open')){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');menu.focus();}});}
  document.querySelectorAll('[data-factory-faq] details').forEach(function(item){item.addEventListener('toggle',function(){if(!item.open)return;document.querySelectorAll('[data-factory-faq] details').forEach(function(other){if(other!==item)other.open=false;});});});
  var data={product:{code:'PRODUCT',title:'先描述产品如何被使用',items:['安装位置与使用环境','外形、接口和内部附件','目标寿命与维护方式']},manufacture:{code:'MAKE',title:'制造条件定义可执行边界',items:['材料、板厚和关键公差','焊接、表面与装配要求','样品及年度预计数量']},delivery:{code:'DELIVER',title:'交付要求完成最后一公里',items:['检验标准与随货记录','包装、标签和认证要求','目的地、贸易条款与交期']}};
  var tabs=[].slice.call(document.querySelectorAll('[data-show-mode]')),panel=document.querySelector('.show-result'),code=document.getElementById('show-code'),title=document.getElementById('show-title'),items=document.getElementById('show-items');
  if(panel){panel.id='show-results';panel.setAttribute('role','tabpanel');panel.setAttribute('aria-live','polite');panel.setAttribute('aria-atomic','true');}
  tabs.forEach(function(tab,index){tab.id='show-mode-'+(index+1);tab.setAttribute('aria-controls','show-results');tab.setAttribute('tabindex',index===0?'0':'-1');});
  function render(key){var item=data[key];if(!item||!code||!title||!items)return;code.textContent=item.code;title.textContent=item.title;items.replaceChildren();item.items.forEach(function(text){var li=document.createElement('li');li.textContent=text;items.appendChild(li);});tabs.forEach(function(tab){var active=tab.getAttribute('data-show-mode')===key;tab.classList.toggle('is-active',active);tab.setAttribute('aria-selected',active?'true':'false');tab.setAttribute('tabindex',active?'0':'-1');if(active&&panel)panel.setAttribute('aria-labelledby',tab.id);});}
  function move(current,event){var index=tabs.indexOf(current),next=index;if(event.key==='ArrowRight'||event.key==='ArrowDown')next=(index+1)%tabs.length;else if(event.key==='ArrowLeft'||event.key==='ArrowUp')next=(index-1+tabs.length)%tabs.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=tabs.length-1;else return;event.preventDefault();render(tabs[next].getAttribute('data-show-mode'));tabs[next].focus();}
  tabs.forEach(function(tab){tab.addEventListener('click',function(){render(tab.getAttribute('data-show-mode'));});tab.addEventListener('keydown',function(event){move(tab,event);});});
  if(tabs.length)render(tabs[0].getAttribute('data-show-mode'));
}());

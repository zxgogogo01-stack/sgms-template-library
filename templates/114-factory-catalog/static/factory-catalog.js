(function(){
  'use strict';
  var menu=document.getElementById('catalog-menu'),nav=document.getElementById('catalog-links');
  if(menu&&nav){menu.addEventListener('click',function(){var open=nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',open?'true':'false');});nav.querySelectorAll('a').forEach(function(link){link.addEventListener('click',function(){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');});});document.addEventListener('keydown',function(event){if(event.key==='Escape'&&nav.classList.contains('is-open')){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');menu.focus();}});}
  document.querySelectorAll('[data-factory-faq] details').forEach(function(item){item.addEventListener('toggle',function(){if(!item.open)return;document.querySelectorAll('[data-factory-faq] details').forEach(function(other){if(other!==item)other.open=false;});});});
  var data={environment:{code:'ENV',title:'环境决定材料与防护起点',items:['室内、户外或沿海区域','温度、湿度和粉尘等级','目标防护与耐腐蚀要求']},structure:{code:'STR',title:'结构参数决定制造边界',items:['外形、安装与开孔尺寸','材质、板厚和特殊公差','门锁、密封与内部附件']},delivery:{code:'DLV',title:'交付条件决定完整报价',items:['样品与年度预计数量','包装、标签和随货文件','目的地、贸易条款与交期']}};
  var tabs=[].slice.call(document.querySelectorAll('[data-catalog-mode]')),panel=document.querySelector('.catalog-result'),code=document.getElementById('catalog-code'),title=document.getElementById('catalog-title'),items=document.getElementById('catalog-items');
  if(panel){panel.id='catalog-results';panel.setAttribute('role','tabpanel');panel.setAttribute('aria-live','polite');panel.setAttribute('aria-atomic','true');}
  tabs.forEach(function(tab,index){tab.id='catalog-mode-'+(index+1);tab.setAttribute('aria-controls','catalog-results');tab.setAttribute('tabindex',index===0?'0':'-1');});
  function render(key){var item=data[key];if(!item||!code||!title||!items)return;code.textContent=item.code;title.textContent=item.title;items.replaceChildren();item.items.forEach(function(text){var li=document.createElement('li');li.textContent=text;items.appendChild(li);});tabs.forEach(function(tab){var active=tab.getAttribute('data-catalog-mode')===key;tab.classList.toggle('is-active',active);tab.setAttribute('aria-selected',active?'true':'false');tab.setAttribute('tabindex',active?'0':'-1');if(active&&panel)panel.setAttribute('aria-labelledby',tab.id);});}
  function move(current,event){var index=tabs.indexOf(current),next=index;if(event.key==='ArrowRight'||event.key==='ArrowDown')next=(index+1)%tabs.length;else if(event.key==='ArrowLeft'||event.key==='ArrowUp')next=(index-1+tabs.length)%tabs.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=tabs.length-1;else return;event.preventDefault();render(tabs[next].getAttribute('data-catalog-mode'));tabs[next].focus();}
  tabs.forEach(function(tab){tab.addEventListener('click',function(){render(tab.getAttribute('data-catalog-mode'));});tab.addEventListener('keydown',function(event){move(tab,event);});});
  if(tabs.length)render(tabs[0].getAttribute('data-catalog-mode'));
}());

(function(){
  'use strict';
  var menu=document.getElementById('sol-menu'),nav=document.getElementById('sol-links');
  if(menu&&nav){menu.addEventListener('click',function(){var open=nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',open?'true':'false');});nav.addEventListener('click',function(event){if(event.target.closest('a')){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');}});document.addEventListener('keydown',function(event){if(event.key==='Escape'&&nav.classList.contains('is-open')){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');menu.focus();}});}
  var data={environment:{code:'ENVIRONMENT / 01',title:'先描述产品在哪里工作',items:[['环境','室内 / 室外、温湿度与腐蚀等级'],['负载','静载、冲击、振动与热源分布'],['维护','安装方向、检修空间与服务周期']]},interface:{code:'INTERFACE / 02',title:'再定义产品如何接入系统',items:[['机械','安装基准、外形限制与连接方式'],['电气','进出线、接地、散热与控制接口'],['人机','操作动线、可视区域与安全防护']]},validation:{code:'VALIDATION / 03',title:'最后确定如何证明它合格',items:[['标准','适用法规、客户规范与判定等级'],['样品','样件状态、数量、工装与测试条件'],['资料','报告、证书、检验记录与追溯周期']]}};
  var tabs=[].slice.call(document.querySelectorAll('[data-sol-mode]')),panel=document.querySelector('.sol-tool-result'),code=document.getElementById('sol-code'),title=document.getElementById('sol-result-title'),items=document.getElementById('sol-items');
  if(panel){panel.id='sol-results';panel.setAttribute('role','tabpanel');panel.setAttribute('aria-live','polite');panel.setAttribute('aria-atomic','true');}
  tabs.forEach(function(tab,index){tab.id='sol-mode-'+(index+1);tab.setAttribute('aria-controls','sol-results');tab.setAttribute('tabindex',index===0?'0':'-1');});
  function render(key){var item=data[key];if(!item||!code||!title||!items)return;code.textContent=item.code;title.textContent=item.title;items.replaceChildren();item.items.forEach(function(row){var li=document.createElement('li'),b=document.createElement('b'),span=document.createElement('span');b.textContent=row[0];span.textContent=row[1];li.append(b,span);items.appendChild(li);});tabs.forEach(function(tab){var active=tab.getAttribute('data-sol-mode')===key;tab.classList.toggle('is-active',active);tab.setAttribute('aria-selected',active?'true':'false');tab.setAttribute('tabindex',active?'0':'-1');if(active&&panel)panel.setAttribute('aria-labelledby',tab.id);});}
  function move(current,event){var index=tabs.indexOf(current),next=index;if(event.key==='ArrowRight'||event.key==='ArrowDown')next=(index+1)%tabs.length;else if(event.key==='ArrowLeft'||event.key==='ArrowUp')next=(index-1+tabs.length)%tabs.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=tabs.length-1;else return;event.preventDefault();render(tabs[next].getAttribute('data-sol-mode'));tabs[next].focus();}
  tabs.forEach(function(tab){tab.addEventListener('click',function(){render(tab.getAttribute('data-sol-mode'));});tab.addEventListener('keydown',function(event){move(tab,event);});});
  if(tabs.length)render(tabs[0].getAttribute('data-sol-mode'));
}());

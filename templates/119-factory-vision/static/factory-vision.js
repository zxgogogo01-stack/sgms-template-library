(function(){
  'use strict';
  var header=document.getElementById('vision-nav'),hero=document.querySelector('[data-fnav-hero]'),menu=document.getElementById('vision-menu'),nav=document.getElementById('vision-links');
  function solid(){if(header&&hero)header.classList.toggle('is-solid',window.scrollY>Math.max(30,hero.offsetHeight-100));}
  if(header&&hero){solid();window.addEventListener('scroll',solid,{passive:true});}
  if(menu&&nav){menu.addEventListener('click',function(){var open=nav.classList.toggle('is-open');menu.setAttribute('aria-expanded',open?'true':'false');});nav.addEventListener('click',function(event){if(event.target.closest('a')){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');}});document.addEventListener('keydown',function(event){if(event.key==='Escape'&&nav.classList.contains('is-open')){nav.classList.remove('is-open');menu.setAttribute('aria-expanded','false');menu.focus();}});}
  document.querySelectorAll('[data-factory-faq] details').forEach(function(item){item.addEventListener('toggle',function(){if(!item.open)return;document.querySelectorAll('[data-factory-faq] details').forEach(function(other){if(other!==item)other.open=false;});});});
  var data={intent:{code:'INTENT / 01',title:'定义产品真正需要坚持的部分',items:['核心体验与使用场景','必须保留的设计语言','成本、周期与性能优先级']},making:{code:'MAKING / 02',title:'把意图转译为稳定的制造秩序',items:['材料、结构与工艺路线','装配基准与接口标准','风险验证与质量控制点']},continuity:{code:'CONTINUITY / 03',title:'让每一次交付延续同一品质',items:['封样、版本与变更管理','批次记录与问题闭环','供应协同与持续改善']}};
  var tabs=[].slice.call(document.querySelectorAll('[data-vision-mode]')),panel=document.querySelector('.vision-result'),code=document.getElementById('vision-code'),title=document.getElementById('vision-result-title'),items=document.getElementById('vision-items');
  if(panel){panel.id='vision-results';panel.setAttribute('role','tabpanel');panel.setAttribute('aria-live','polite');panel.setAttribute('aria-atomic','true');}
  tabs.forEach(function(tab,index){tab.id='vision-mode-'+(index+1);tab.setAttribute('aria-controls','vision-results');tab.setAttribute('tabindex',index===0?'0':'-1');});
  function render(key){var item=data[key];if(!item||!code||!title||!items)return;code.textContent=item.code;title.textContent=item.title;items.replaceChildren();item.items.forEach(function(value){var li=document.createElement('li');li.textContent=value;items.appendChild(li);});tabs.forEach(function(tab){var active=tab.getAttribute('data-vision-mode')===key;tab.classList.toggle('is-active',active);tab.setAttribute('aria-selected',active?'true':'false');tab.setAttribute('tabindex',active?'0':'-1');if(active&&panel)panel.setAttribute('aria-labelledby',tab.id);});}
  function move(current,event){var index=tabs.indexOf(current),next=index;if(event.key==='ArrowRight'||event.key==='ArrowDown')next=(index+1)%tabs.length;else if(event.key==='ArrowLeft'||event.key==='ArrowUp')next=(index-1+tabs.length)%tabs.length;else if(event.key==='Home')next=0;else if(event.key==='End')next=tabs.length-1;else return;event.preventDefault();render(tabs[next].getAttribute('data-vision-mode'));tabs[next].focus();}
  tabs.forEach(function(tab){tab.addEventListener('click',function(){render(tab.getAttribute('data-vision-mode'));});tab.addEventListener('keydown',function(event){move(tab,event);});});
  if(tabs.length)render(tabs[0].getAttribute('data-vision-mode'));
}());
